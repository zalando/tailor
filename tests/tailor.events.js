'use strict';

const assert = require('assert');
const http = require('http');
const nock = require('nock');
const sinon = require('sinon');
const Tailor = require('../index');

describe('Tailor events', () => {

    it('proxies scoped events from fragments');

    it('emits `start` event');

    it('emits `template:error` event');

    it('emits `context:error` event');

    it('emits `primary:error` event');

    it('emits `primary:timeout` event');

});
